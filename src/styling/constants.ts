export const colors = {
  primary: "#00bfa5",
  error: "#d50000"
}

const deviceSizes = {
  desktop: "500px"
}

export const mediaQueries = {
  mobile: `(max-width: ${deviceSizes.desktop})`,
  desktop: `(min-width: ${deviceSizes.desktop})`
}
